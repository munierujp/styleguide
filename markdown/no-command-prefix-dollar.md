# no-command-prefix-dollar

コードブロックのコマンドの先頭に`$`をつけないでください。コピーしたコマンドをそのままペーストして実行する際に邪魔になります。

## :thumbsdown: 悪い例

````markdown
```
$ echo hello
```
````

## :thumbsup: 良い例

````markdown
```
echo hello
```
````
